using System;
using System.Collections.Generic;
using System.Text;

namespace DataStructuresAssignment
{
    class LinkedList
    {
        Node head;
        public int Search(int value)
        {
            int count = 0;
            Node temp = this.head;
            while (temp != null)
            {
                
                Console.WriteLine();
            }
            return count;
        }
    }
}
